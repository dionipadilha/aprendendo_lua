-- tool.lua

local Class = require "class"

-- extending the capabilities of agents

local Tool = Class:new {
  name = "",
  description = "",
  run = "No function defined to run."
}

return Tool
